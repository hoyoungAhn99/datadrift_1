extends: icarl_bgs_a7.yaml

# A7 insertion targets are unchanged.  Only conflicting insertion gradients
# are projected off the CE+KD+boundary-preservation stability gradient.
method:
  boundary_graph_surgery:
    insertion:
      gradient_projection: true
      projection_epsilon: 1.0e-12

output:
  run_name: icarl_bgs_pcli_l16_l1
