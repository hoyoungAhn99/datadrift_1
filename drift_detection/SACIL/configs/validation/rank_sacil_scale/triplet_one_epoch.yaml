extends: ../../ablations/cifar100/icarl_sacil_rank_triplet.yaml

training:
  incremental:
    epochs: 1
    milestones: []

debug:
  max_sessions: 2

output:
  directory: outputs/validation/rank_sacil_scale
  run_name: triplet_l1_one_epoch
