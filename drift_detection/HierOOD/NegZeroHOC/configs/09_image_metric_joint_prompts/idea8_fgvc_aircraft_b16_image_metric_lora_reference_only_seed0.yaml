base_config: idea8_fgvc_aircraft_b16_image_metric_lora.yaml

experiment:
  name: idea8-fgvc-aircraft-image-metric-reference-only-seed0

runtime:
  gpu_id: 0
  seed: 0
  deterministic: true

dataloader:
  num_workers: 0

image_metric_training:
  training_split:
    reference_only: true
    reference_fraction: 0.8
  checkpoint: outputs/experiments/idea8-fgvc-aircraft-image-metric-reference-only-seed0/checkpoints/image-metric.pt
  validation:
    last_checkpoint: outputs/experiments/idea8-fgvc-aircraft-image-metric-reference-only-seed0/checkpoints/image-metric-last.pt
  diagnostics_path: outputs/experiments/idea8-fgvc-aircraft-image-metric-reference-only-seed0/diagnostics/image-metric.json
